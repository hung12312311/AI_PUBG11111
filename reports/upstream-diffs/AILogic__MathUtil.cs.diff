--- Custom/AILogic/MathUtil.cs

+++ Upstream/AILogic/MathUtil.cs

@@ -1,5 +1,3 @@

 using Aimmy2.AILogic;
-using System;
-using System.Collections.Generic;
 using System.Drawing;
 using System.Drawing.Imaging;
@@ -124,5 +122,6 @@

                     float* bPtr = dest + bOffset;
 
-                    for (int y = 0; y < height; y++)
+                    // process rows in parallel (avoid creating 640 threads)
+                    Parallel.For(0, height, new ParallelOptions { MaxDegreeOfParallelism = 4 }, (y) =>
                     {
                         byte* row = basePtr + (long)y * stride;
@@ -159,4 +158,6 @@

                             gPtr[baseIdx + 3] = _byteToFloatLut[p[13]];
                             rPtr[baseIdx + 3] = _byteToFloatLut[p[14]];
+
+                            p += 16; // move pointer 16 bytes forward (4 pixels * 4 bytes per pixel)
                         }
 
@@ -172,5 +173,5 @@

                             rPtr[idx] = _byteToFloatLut[p[2]];
                         }
-                    }
+                    });
                 }
             }
@@ -181,142 +182,4 @@

             }
         }
-
-        /// <summary>
-        /// Converts DirectX mapped staging texture memory directly to float array (Tensor format).
-        /// Bypasses creating a GDI+ Bitmap entirely.
-        /// </summary>
-        public static unsafe void DxMapToFloatArray(byte* src, int srcStride, float[] result, int IMAGE_SIZE, bool thirdPersonSupport)
-        {
-            if (result == null) throw new ArgumentNullException(nameof(result));
-
-            int width = IMAGE_SIZE;
-            int height = IMAGE_SIZE;
-            int totalPixels = width * height;
-
-            if (result.Length != 3 * totalPixels)
-                throw new ArgumentException($"result must be length {3 * totalPixels}", nameof(result));
-
-            const int bytesPerPixel = 4;
-            const int pixelsPerIteration = 4;
-
-            int rOffset = 0;
-            int gOffset = totalPixels;
-            int bOffset = totalPixels * 2;
-
-            // Third person mask region boundaries
-            int tpWidth = width / 2;
-            int tpHeight = height / 2;
-            int tpStartY = height - tpHeight;
-
-            fixed (float* dest = result)
-            {
-                float* rPtr = dest + rOffset;
-                float* gPtr = dest + gOffset;
-                float* bPtr = dest + bOffset;
-
-                for (int y = 0; y < height; y++)
-                {
-                    byte* row = src + (long)y * srcStride;
-                    int rowStart = y * width;
-                    bool inTpYRange = thirdPersonSupport && (y >= tpStartY);
-
-                    int x = 0;
-                    int widthLimit = width - pixelsPerIteration + 1;
-                    for (; x < widthLimit; x += pixelsPerIteration)
-                    {
-                        int baseIdx = rowStart + x;
-                        byte* p = row + (x * bytesPerPixel);
-
-                        if (inTpYRange && x < tpWidth)
-                        {
-                            // Mask pixel 0
-                            bPtr[baseIdx] = 0.0f;
-                            gPtr[baseIdx] = 0.0f;
-                            rPtr[baseIdx] = 0.0f;
-
-                            // Mask pixel 1 if it falls in the mask region
-                            if (x + 1 < tpWidth)
-                            {
-                                bPtr[baseIdx + 1] = 0.0f;
-                                gPtr[baseIdx + 1] = 0.0f;
-                                rPtr[baseIdx + 1] = 0.0f;
-                            }
-                            else
-                            {
-                                bPtr[baseIdx + 1] = _byteToFloatLut[p[4]];
-                                gPtr[baseIdx + 1] = _byteToFloatLut[p[5]];
-                                rPtr[baseIdx + 1] = _byteToFloatLut[p[6]];
-                            }
-
-                            // Mask pixel 2 if it falls in the mask region
-                            if (x + 2 < tpWidth)
-                            {
-                                bPtr[baseIdx + 2] = 0.0f;
-                                gPtr[baseIdx + 2] = 0.0f;
-                                rPtr[baseIdx + 2] = 0.0f;
-                            }
-                            else
-                            {
-                                bPtr[baseIdx + 2] = _byteToFloatLut[p[8]];
-                                gPtr[baseIdx + 2] = _byteToFloatLut[p[9]];
-                                rPtr[baseIdx + 2] = _byteToFloatLut[p[10]];
-                            }
-
-                            // Mask pixel 3 if it falls in the mask region
-                            if (x + 3 < tpWidth)
-                            {
-                                bPtr[baseIdx + 3] = 0.0f;
-                                gPtr[baseIdx + 3] = 0.0f;
-                                rPtr[baseIdx + 3] = 0.0f;
-                            }
-                            else
-                            {
-                                bPtr[baseIdx + 3] = _byteToFloatLut[p[12]];
-                                gPtr[baseIdx + 3] = _byteToFloatLut[p[13]];
-                                rPtr[baseIdx + 3] = _byteToFloatLut[p[14]];
-                            }
-                        }
-                        else
-                        {
-                            // Standard BGR to RGB conversion
-                            bPtr[baseIdx] = _byteToFloatLut[p[0]];
-                            gPtr[baseIdx] = _byteToFloatLut[p[1]];
-                            rPtr[baseIdx] = _byteToFloatLut[p[2]];
-
-                            bPtr[baseIdx + 1] = _byteToFloatLut[p[4]];
-                            gPtr[baseIdx + 1] = _byteToFloatLut[p[5]];
-                            rPtr[baseIdx + 1] = _byteToFloatLut[p[6]];
-
-                            bPtr[baseIdx + 2] = _byteToFloatLut[p[8]];
-                            gPtr[baseIdx + 2] = _byteToFloatLut[p[9]];
-                            rPtr[baseIdx + 2] = _byteToFloatLut[p[10]];
-
-                            bPtr[baseIdx + 3] = _byteToFloatLut[p[12]];
-                            gPtr[baseIdx + 3] = _byteToFloatLut[p[13]];
-                            rPtr[baseIdx + 3] = _byteToFloatLut[p[14]];
-                        }
-                    }
-
-                    for (; x < width; x++)
-                    {
-                        int idx = rowStart + x;
-                        byte* p = row + (x * bytesPerPixel);
-
-                        if (inTpYRange && x < tpWidth)
-                        {
-                            bPtr[idx] = 0.0f;
-                            gPtr[idx] = 0.0f;
-                            rPtr[idx] = 0.0f;
-                        }
-                        else
-                        {
-                            bPtr[idx] = _byteToFloatLut[p[0]];
-                            gPtr[idx] = _byteToFloatLut[p[1]];
-                            rPtr[idx] = _byteToFloatLut[p[2]];
-                        }
-                    }
-                }
-            }
-        }
     }
 }